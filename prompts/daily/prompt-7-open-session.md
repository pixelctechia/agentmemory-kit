# Prompt 7 — Open a session

Use this whenever the global orchestrator hasn't already surfaced the
project's memory status automatically (e.g. tools without a hook system,
or the very first message of a manual session).

```text
Before doing anything else, read this project's memory: start with memory/CURRENT_STATE.md, then AGENTS.md for the permanent rules and source-of-truth hierarchy, then memory/17_GRAPHIFY_INDEX.md to understand what the knowledge graph covers. Do not read every numbered memory file unless needed for the task at hand — pull in specific files only if relevant.

After reading, confirm: what this project does, what was last worked on, what's in progress, and whether the knowledge graph is current or stale. Then wait for my instructions before making any changes.
```
